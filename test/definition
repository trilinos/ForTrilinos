

#
# Main Thyra tests
#

(FRAMEWORK, INSTALL) {
    TESTS = ./simpleEpetraExteranCInterface.exe;
    ARGS = ;
    COMM = MPI,SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = ./simpleEpetraFortranInterface.exe;
    ARGS = ;
    COMM = MPI,SERIAL;
}

